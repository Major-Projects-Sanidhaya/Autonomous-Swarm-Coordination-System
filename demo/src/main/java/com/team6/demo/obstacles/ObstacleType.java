package com.team6.demo.obstacles;

/**
 * ObstacleType - Categories of obstacles
 */
public enum ObstacleType {
    STATIC,    // Does not move (buildings, terrain)
    DYNAMIC    // Moves over time (vehicles, other agents)
}
